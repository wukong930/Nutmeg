# 定时体检的「已知红灯」基线 —— scripts/health_check_cron.sh 读它。
#
# 每行一条:`<节标题> | <fail 判词>`。在这个清单里的红灯**不弹通知**
# (报告 logs/health_check_latest.md 里照样写)。不在清单里的红 ⇒ 🔴 弹窗。
# 清单里有、但这次没红了 ⇒ 🟢 弹窗提醒你**把它从这里删掉** ——
# 否则基线会永远保护一个已经修好的条目,同名问题复发时被静默吞掉。
#
# ⚠️ 加一条进来 = 明确决定「这个红我暂时接受」。请连同**理由和退出条件**一起写,
#    不要只贴一行。没有退出条件的豁免会变成永久失明。
# 🚨 §18 服务盘一致性(artifact identity)**不受本清单约束** —— 它是钱路,
#    在这里加它也没用,脚本会照报。这是有意的。

# ── 2026-08-07 建立基线时的现状 ──────────────────────────────────
#
# 【为什么接受】装定时体检的当天,health_check.sh 就 exit 1,红在这一条上。
#   若按 exit code 报警,从次日起每天一条同样的弹窗 ⇒ 三天内被关掉 ⇒
#   连带真正要紧的服务盘告警一起失效。噪音告警等于没有告警。
#
# 【具体内容】(2026-08-07 实测)
#   • JPN_J2              AF_id ✓ · sport_key ✗ · 队表 40 队 · zh 字典 ✓ 全可达
#   • NED_EERSTE_DIVISIE  AF_id ✓ · sport_key ✗ · 队表 20 队 · zh 字典 ✗ 4 打不中
#     打不中的 4 队:Jong AZ, Jong Ajax, Jong PSV U21, Jong Utrecht
#
# 【退出条件】两件事任一完成就该重跑体检并把本条删掉:
#   1. 给 JPN_J2 / NED_EERSTE_DIVISIE 补上 Odds API 的 sport_key(须 live 验证,
#      别照猜 —— 见 W2-1 补 8 键那次的做法)。
#   2. 补齐那 4 支荷乙青年队的中文译名。⛔ 受「绝不照英文猜译名」红线约束:
#      错映射会静默污染 join,比缺映射更坏(缺了至少哨兵会响)。正解是等竞彩
#      上架后照抄官方写法,或用 sporttery.py 那套「同日 + 已确认对手当锚」推出来。
#
# 【风险自陈】本条的键是通用判词,所以**换一个联赛出同类问题也不会新增报警**。
#   这是取舍不是疏忽,原因写在 scripts/health_check_cron.sh 的「已知的局限」段。
#
# ── 2026-08-18 移除:#10 注册表覆盖率(registry-coverage)──────────────
#   之前接受「注册表有硬缺口 — 某切片已静默降级」。本次体检该切片已无硬缺口
#   (只剩 3 条「待发布队表」警告),按基线机制删除本条,以免复发被静默吞掉。
