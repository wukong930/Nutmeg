# 秋季软水测量 · 预注册分析计划(Pre-Registration)

**状态:🔒 v1.6 · 2026-07-15**(v1.0 于 2026-07-02 锁定;修订见 §9;修订均先于其所辖数据)— 前向窗口检验(§2–§5)在受训联赛复赛(~2026-08)前写死;**§H 历史 held-out 分析于 2026-07-09 先于任何历史读数写死,并已於同日单次读完(判决见 `docs/jingcai_history_campaign_2026-07-09.md`:软水历史侧 null)**。v1.6 仅更正皇冠代理误差的实测值 + 登记备用锚就绪,**未改任何检验定义**。
**归属**:`docs/autumn_restart_checklist.md` §0 的收尾件;§1 全部测量的确认性/探索性边界以本文件为准。

---

## 0 · 为什么要预注册

CLV 闸门(`nutmeg.v4.model.clv_gate`)已经管住了**联赛间**的多重检验(BHY-FDR)。本文件管住更大的自由度:**分析菜单本身**。秋季数据可以按 联赛 × 散户切片 × 冻结缺口 × 玩法 × 时点 × 口径 切出上百种组合——事后翻数据、翻到显著为止,FDR 也救不了(family 根本没定义)。这就是 forking paths / garden of forking paths。

解法与临床试验相同:**在数据到达之前声明**哪些检验是「确认性」(有资格触发资金)、哪些是「探索性」(只准生成下季假设),并锁定口径。预注册的效力恰恰来自它先于数据存在。

**修订纪律**:数据开始到达(首条窗口内选中腿)之后,对 §2–§5(确认性部分)的任何修改必须:① 记入 §9 Changelog(日期+理由);② 只对「修改之后才达到读数条件」的检验生效——**禁止改完口径去读已攒的数据**。§6(探索性)随便改。

---

## 1 · 冻结的仪器(normative = 代码;本节只登记,不复述公式)

| 件 | 位置 | 锁定值 |
|---|---|---|
| 选中腿定义 | `nutmeg.v4.cli.clv_ledger.compute_ledger` | 每 `(match, market∈{had,hhad})` 取**最新捕获**;3 路按**捕获时** Pinnacle 公允(WPO 去vig;hhad 由捕获 1X2+O/U DC 反推 cover-P)算 EV;argmax 腿 EV ≥ `_MIN_EV=0.05` 入选。无前视。 |
| CLV | 同上 | `P_close(WPO) × 竞彩SP − 1`;close 来自 `odds_snapshots`(closing cron)经 `_pinn_close`。 |
| 检验统计 | `nutmeg.v4.model.clv_gate` | 按**比赛日聚类**的稳健单边 t(mean CLV > 0);`WARN_MIN_N=15`;confirm = `t≥2.8 (CONFIRM_T)` 且 `N≥200 (CONFIRM_MIN_N)` 且过 `BHY-FDR q=0.05 (FDR_Q)`。 |
| 合并读数 | `clv_gate.pooled_test` | 联赛命名空间化聚类;**仅参考,永不作闸门**。 |
| 联赛标签 | `nutmeg.v4.data.league_labels.canonical_league` | 跨 writer 归一(sporttery 中文缩写 + 记一笔 EN 码 → 一个组);fail-open。 |

以上常数若未来修改 = §9 记录 + 只对未读数据生效。

## 2 · 总体(population)

- **窗口**:`2026-08-01 ≤ match_date ≤ 2027-06-30`(日职为历年制,窗口内场次照常纳入)。
- **市场**:had(胜平负)+ hhad(让球胜平负),竞彩官方 SP。
- **确认性联赛全集(13,= 生产 artifact 训练面,`TRAINED_LEAGUES_CN`)**:英超 英冠 西甲 西乙 德甲 德乙 意甲 意乙 法甲 法乙 荷甲 葡超 日职。
- **确认性排除**:
  1. 非上述联赛(世界杯/友谊赛/杯赛/北欧等)→ 只进探索性;
  2. hhad 行捕获缺 O/U(`psc_over/psc_under` NULL → 公允退化用默认总进球)→ 从确认性剔除,**剔除数公开**;
  3. `no_close` 腿天然无 CLV;但**读数有效性前置**:该联赛窗口内 `miss_name`(疑似队名别名断裂)未解决数必须为 0——修别名、重算、再读。

## 3 · 确认性检验(动钱的唯一入口)

- **P1 · 合并证据(1 个检验)**:13 联赛选中腿合并(`pooled_test`),H1: mean CLV > 0。
  达标条件:`t≥2.8` 且 `N≥200`。**语义 = 证据声明**(「竞彩受训面存在软水」),**不解锁任何联赛**——合并显著可能由单一联赛驱动。
- **P2 · 逐联赛闸门(family ≤13)**:即 `clv_gate.evaluate()` 现行输出。
  **confirm**(`t≥2.8` ∧ `N≥200` ∧ 过 BHY-FDR q=0.05)⇒ 该联赛进入 §5 解锁流程。
  **warn**(`N≥15` ∧ mean>0)⇒ 值得盯,不动钱。
- **正式读数节点**:以 `weekly_gate` cron 周报输出为准。闸门的每日输出=监控;`CONFIRM_T=2.8`(单边 p≈0.005)的量级即为吸收持续监控的反复窥视而设(Harvey-Liu-Zhu 对重复筛选环境的门槛)。

## 4 · 次级预指定检验(不解锁资金;过检 ⇒ 升级为下季确认性 / 按注明调整规则)

自成一个 BHY family(q=0.05,m=6;S6 于 2026-07-04 加入,先于窗口数据)。方法注明为规格,实现代码秋季随数据补——**先于数据声明的是假设与判读规则**:

| # | 检验 | 操作定义 | H1 |
|---|---|---|---|
| S1 | 冻结缺口 | 选中腿按 `T_gap = kickoff_utc − 最后捕获时刻` 四分位;Q4−Q1 mean CLV 差,cluster-mean Welch t(聚类=比赛日)。规格源:`docs/freeze_gap_test_card.md` | 缝随陈旧度增大(深夜欧洲场最大) |
| S2 | 散户逆向 | join `jingcai_vote`(队名+日期);选中腿二分「被选方向 vs supportRate 最高方:逆/顺」;逆−顺 mean CLV 差,同上 | 逆散户组 CLV 更高 |
| S3 | 玩法差 | hhad − had 选中腿 mean CLV 差,同上 | 让球更软(散户更看不懂) |
| S4 | 初盘 vs 终盘 | 同场同市场且 `jc_open_*` 存在:选择重放于初盘 SP;配对差(终−初),比赛日聚类 t | 方向未定(哪端更划算是开放问题) |
| S5 | 方差门槛(B2) | 同窗口选择重放于 `门槛=5%+z·σ_P·SP`(参数按 `docs/ev_threshold_variance_2026-06-26.md`)vs flat-5%:两组 mean CLV + 选中量对比 | 方差门槛组假 +EV 更少 |
| S6 | 让球切分偏差复现 | 窗口内(≥2026-08-01)自家缓存已结束场,**逐字复用发现口径**(`docs/ah_vs_grid_three_way_backtest_2026-07-04.md` 附录:Pinnacle 1X2 WPO + 真实 ou_line 网格;整数线 h 取相邻纯半球线都被报价者;**限 \|h\|=1**);逐样本残差 `r = 1{让平} − P_grid(让平)`,H1 单边、比赛日聚类 t;让胜残差(预期镜像为负)与让负残差(预期 ≈0,锚完好性检查)随报告并列 | 让平低估复现(E[r] > 0,发现值 +3.1pp) |

**S5 特别条款**(呼应 checklist §2-B2):S5 过 FDR 且方向为优 ⇒ B2 门槛才允许替换 flat-5% 进下注规则;否则 flat-5% 沿用。

**S6 特别条款(修正式 C1 冻结 + 部署闸)**:候选修正式于本修订日冻结为 **C1:仅 `|handicap_home| = 1` 的让球三路,`P′(让胜)=P(让胜)−δ`,`P′(让平)=P(让平)+δ`,`δ=0.028`,让负不动**(来源:2026-07-04 in-sample,\|h\|=1 N=1,333,让胜 +2.8pp/让平 −3.1pp/让负 ±0,≈2.5σ;δ 取让胜侧较小值,质量守恒)。**部署条件 = S6 过本 family FDR 且符号一致且让负残差无异动**,过检后 C1 方可进服务端(展示 + EV 口径,含 CACHE_VERSION/前端同步);未过检 ⇒ 纯网格沿用,C1 作废重估。**禁止**:秋季按新数据改 δ 再宣称「已验证」——那是用同一数据估计+检验;若 δ 明显偏,判定为「方向复现、量级未定」,C1 携新 δ 进下下季。附排除项:rho 已实测为反方向旋钮(−0.10→−0.16 让平再降 0.5pp),亚盘直读让平更低,均不得作为替代方案重提。功率注记:按发现样本速率,一季 \|h\|=1 约 N≈900–1,100,δ≈3pp ⇒ t≈2 量级,**边缘**;容许跨季累计读数(读数节点=季末,或 N≥800 后的任一 weekly 周报,先到者)。意识层缓解(非规范,即刻可用):\|h\|=1 让胜腿 EV 读数心算 −5pp,让平腿 EV 读数视为保守下限。

## 5 · 解锁与止损(P2 confirm 之后)

1. **一次性人工审计**(读数生效前 checklist):该联赛 `miss_name=0`;N/聚类数复核;tier 分布无冷门堆积(防 σ_EV 陷阱);S 系列无反向警报;`data_freshness` 全绿。
2. **解锁范围**:仅该联赛、仅 had/hhad 单关;**半 Kelly**;单注上限沿用现行规则;门槛按 S5 结论(默认 flat-5%)。
3. **预注册止损(kill 规则)**:解锁日起新增选中腿累计 `N≥100` 且 mean CLV < 0 ⇒ **重新锁定**该联赛,回 warn 状态,写复盘后才可再议。
4. **全季无 confirm**(至 2027-06-30)⇒ 判「本季受训面无可证软水」:§2 机器保持锁定,空仓是正确产出;下一步(转向/缩规模/续攒)另行决策,不在本文件预设。

## 6 · 探索性(无资格动钱)

其余一切切片(tier/rank/主客/时段/非受训联赛/竞彩异型盘/…)均为探索性:报告中必须标注 `exploratory`,只准生成下季假设;若想升级为确认性,须在**下一季窗口开始前**修订本文件(§9 留痕)。

## 7 · 功率诚实注记

- Kaunitz 基准:一个**真 +EV** 策略在 672 注时仅 p≈0.089 ⇒ **单联赛一季很可能到不了 confirm**,这不是失败,是样本量的物理。
- P1(合并)是本季功率最高的读数;P2 的 confirm 更可能出现在选中腿产量高的联赛。
- 「全季只有 warn」是完全可能且诚实的结局 = 继续攒或转向的依据,**不得**因此回头放松 §1 的常数(那正是本文件要防的事)。

## 8 · 数据质量前置(任何读数生效的条件)

- `data_freshness` 哨兵绿(`jingcai_sp` / `jingcai_vote` / `odds_snapshots`);
- `com.nutmeg.sporttery_open`(11:05 初盘)与三窗 vote cron 正常(S4/S2 的供给);
- **标签审计**:读数前跑 `SELECT DISTINCT league FROM jingcai_sp` 核对——出现 `league_labels` 未收录的同义标签 ⇒ 先补映射再读(fail-open 设计会让新同义词自成一组,可见不可漏);
- hhad 退化行(缺 O/U)剔除数随报告公开。

## H · 历史 held-out 分析(2026-07-09 声明,先于任何历史读数)

**H.0 · 理据与效力.** 2026-07-09 发现官方 gateway `getFixedBonusV1.qry?matchId=` 公开**历史**竞彩全程赔率走势(胜平负/让球/比分/总进球/半全场,~2017→今,plain curl 无墙;`记忆 jingcai-fixedbonus-history-endpoint`)——推翻「竞彩 SP forward-only」。本 §H 把这批新到手历史数据纳入一次**预指定 held-out 检验**,与前向窗口(§2–§5)**并行、不替代、不互污染**。效力来源:①本节复用的**全部仪器**(`clv_gate` 常数、`_MIN_EV`、C1 `δ=0.028`、S6 残差公式、`canonical_league`)均在 **07-02~07-05 锁定,早于 07-09 发现本源** → 无「在此数据上调参」;②**单次预提交读数**(历史=固定集,非序列窥视);③本节先于任何历史读数写死。**诚实边界(非 pristine)**:软水假设与 13 联赛选择受过与该期**部分重叠**的先验(WC 2018/22、芬超、0/33 前向小样本)→ §H = **强预指定证据,非终审**;重叠限于那批小样本,8 年绝大多数是从未见过的俱乐部场,泄漏有限、登记在案。

**H.1 · 钱纪律(与前向 P2 不同,关键).** 历史 H-P2 confirm **不直接解锁资金**:①历史 ≠ 预注册的前向钱窗口;②收盘用 500 皇冠盘代理 Pinnacle(**误差实测 v1.6 更正:中位 0.61pp、系统偏差 ≤0.4pp**;原记「1–4pp」是尾部);③非 pristine + 制度漂移。故 **历史 confirm ⇒「高先验候选联赛」→ 喂前向窗口 + §2 引擎设计 + 供功率/effect-size;真金仍闸在前向 P2(§3/§5 原样冻结)**。吃满功率红利,守住钱闸。

**H.2 · 总体.**
- **主窗口** `2021-08-01 ≤ match_date ≤ 2025-07-31`(近 4 季,最相关+最平稳);**次级/稳健** 全档案(~2017→2025-07)。**逐季报 CLV** 查制度漂移;主结论以主窗口为准。
- **市场/联赛**:had+hhad;13 受训联赛(`TRAINED_LEAGUES_CN`,同 §2)。
- **收盘基准**:`_pinn_close`(odds_snapshots)优先,历史缺口用 **500 皇冠盘归档**(Crown≈Pinnacle 收盘:**实测中位 0.61pp / 偏差 ≤0.4pp**,配对 bootstrap 判打平 —— 见 v1.6 与 `docs/crown_anchor_evaluation_2026-07-15.md`;旧记「1–4pp」= 尾部);收盘源逐腿标注、皇冠代理误差随报告公开。
- **排除**:①非 13 联赛→仅探索;②hhad 缺 O/U→剔(数公开);③读数前置:该联赛 `miss_name`(中文↔Pinnacle/500 别名断裂)=0 才可读(修→重算→再读,同 §2)。

**H.3 · 确认性(镜像 §3).**
- **H-P1 合并**:13 联赛 `pooled_test`,H1 mean CLV>0,`t≥2.8 ∧ N≥200`。=证据声明,不点名联赛。
- **H-P2 逐联赛**:`clv_gate.evaluate()`,BHY-FDR family=13;confirm=`t≥2.8 ∧ N≥200 ∧ 过FDR`。(8 年 N 极大→实际卡在**符号为正+t**;单次读数下 2.8 更保守 = 任何 confirm 极稳健。)

**H.4 · 次级预指定(BHY family m=5;S2 排除:vote 无历史).**
| # | 检验 | 历史操作定义 | H1 |
|---|---|---|---|
| H-S1′ | 冻结缺口(改写) | 历史 T_gap≈恒定(竞彩总在开赛前~5min 封)→无法按 §5-S1 四分位切;改按**开球时段**(深夜欧洲 vs 其他)切 mean CLV,聚类=比赛日 t | 深夜欧洲场 CLV 更高 |
| H-S3 | 玩法差 | hhad−had 选中腿 mean CLV 差 | 让球更软 |
| H-S4 | 初盘 vs 终盘 | hadList 首行(初)vs 末行(终)重放选择,配对差(终−初),比赛日聚类 t | 方向未定 |
| H-S5v | 方差门槛 B2 | 历史选择重放于 `门槛=5%+z·σ_P·SP` vs flat-5%,mean CLV+选中量对比 | 方差门槛组假+EV 更少 |
| H-S6 | 让平/C1 验证 | `split_bias`/`nutmeg-s6-split-check` 主窗口 \|h\|=1 让球残差:**检验冻结 C1(δ=0.028、方向)held-out 是否成立**;**另报**历史 δ 点估+CI(=前向 δ 精修候选,按 §4-S6 特别条款可先于前向窗口修订;**test 与 estimate 严格分开**) | 让平低估复现 E[r]>0、C1 符号成立、让负锚≈0 |

**H.5 · 读数与止损纪律.**
1. **单次预提交读数**:Phase 1 回填 + 覆盖率审计 + 各联赛 `miss_name=0` 后**按本 §H 规格跑一次**;**禁 tweak-and-rerun**(改规格=新预注册,不对已读历史生效)。
2. **覆盖率审计前置**:报 getFixedBonus 各联赛各季覆盖 / 竞彩应有场次;系统性缺失(非 MCAR)→标记选择偏差、结论降级。
3. **历史 confirm ≠ 钱**(H.1)、**≠ 前向窗口**;前向 §2–§5 不受本节影响。
4. **诚实先验**:给定 0/33 + −9~−11% vig 墙,**最可能结局 = H-P1 高功率不拒(CLV 深负、无软水)= 强证据结束 campaign**——不是失败,是**现在就拿到的高功率诚实答案**;预先声明以防把预期中的 null 当挫败。

**H.6 · 探索性(禁动钱,同 §6).** vig 阶梯随时间、竞彩线是否系统向收盘漂、overround 带切片(§1⑦)、异型盘 EV(比分/总进球,用**市场派生 P 非模型 P** 避 look-ahead)、单关标记×软水。只生成假设。

---

## 9 · Changelog

- **v1.6 · 2026-07-15**:**皇冠代理误差实测更正 + 备用锚就绪(非规格变更;§2–§5 与 §H 定义未动一字)**。
  ① **更正 §H 的「1–4pp」为实测值**:该数字系旧记忆的**尾部**,非典型值。今日在 **N=1,067 场**两源重叠(WPO 去vig、比分 100% 一致验证 join)实测:**系统偏差 主胜 +0.04pp / 平 −0.40pp / 客胜 +0.36pp**,**绝对差中位 0.61pp**(90 分位 1.7pp,最大 4.9pp)。另两个独立配对 bootstrap 判**打平**:线本身 Pinnacle 0.9674 vs 皇冠 0.9683(95% CI [−0.0014, +0.0031]);Pinnacle 训的模型**服务时**改喂皇冠 0.9594 vs 0.9604(CI [−0.0006, +0.0026])。⇒ **皇冠代理比 §H 当初假设的更可信**(H.1 的钱纪律仍**原样**:历史 confirm 不解锁资金)。**方向**:此更正只让既有 §H 结论(软水历史侧 null)**更稳**,不产生任何新读数。源:`docs/crown_anchor_evaluation_2026-07-15.md`。
  ② **皇冠 fallback 未接进 `clv_ledger` —— 因为没有缺口**。实跑当日账本:配对 357 场、**「无收盘线 0」**(名字不配 0 / 真没报价 0 / 去vig失败 0)= `_pinn_close`(odds_snapshots)覆盖 **100%** ⇒ 回退分支**永不触发**,不写死代码。§H/第 99 行「历史缺口用皇冠」的规格**不变**,只是当下无缺口可填。
  ③ **今日回填的 2026-01-14→07-15(2,104 场)不喂任何读数**。§H 已於 **v1.3(07-09)单次预提交读数完毕**、软水判决已下 ⇒ 拿新数据重读同一假设族 = **第二次窥视,禁止**。该批数据的定位 = **前向保险**:[[odds-crons-paused-quota-2026-07]] 仍暂停,且 football-data 的 Pinnacle 已於 2026-02 永久断供(`记忆 pinnacle-dead-in-footballdata-2026-01`)⇒ 若秋季 odds cron 未能恢复,**皇冠是唯一可得的收盘锚**;今日已把这条备用锚修通(见 ④),到时可直接启用。**但这不改变**「复赛前必须恢复 odds cron」——实盘 EV 要**赛前活线**,500 只有赛后档案。
  ④ **三个工程修(`8f59b22`,非规格)**:让皇冠这条备用锚从「配得上 58.9%」到 **97.2%**。(a) **日期语义**:500 XML 的 `matchnumdate`=竞彩期号日 ≠ 开赛日;新增 `kickoff_utc`(北京 `matchdate`+`matchtime` → UTC)对齐 `jingcai_sp` 的 UTC 开赛日;**`match_date` 不动**(它恰好对得上 football-data 的当地日期)。(b) **17 个 500 中文变体**(北欧为主),用「同 `kickoff_utc` + 对手为已确认锚」**推**出、0 歧义,非模糊匹配。(c) **真 bug**:upsert 的 `DO UPDATE SET` 漏了 `home_team/away_team` ⇒ 补词典**永远传不到已有行**(补完 17 个别名 join 一场没涨才暴露);改 `COALESCE(新, 旧)`。
- **v1.5 · 2026-07-09**:**C1 补 +1 线镜像修正**(热门在客):+1 线 `P′(让负)=P(让负)−δ₊₁`、`P′(让平)=P(让平)+δ₊₁`、`δ₊₁=0.013`,让胜不动(`_C1_DELTA_P1`,`implied_handicap_lines` 同处;LIVE 可验)。理据:+1 底层偏差 DC−市场让负 **+1.27pp(t=20.2)** —— 与 −1 的让胜 +1.57pp/t=31.5 **同量级**;爆盘尾高估是**对称现象**(−1 热门在主=让胜 over / +1 热门在客=让负 over)。**诚实弱确认**:样本外 3-way log-loss 配对 **t=+1.89**(方向对但 <2.8,功率受限:log-loss 对小 P 位移钝 + +1 测试 N=554 仅 −1 一半)→ **凭 t=20 的底层偏差 + 对称镜像原理部署,非凭 OOS-log-loss**(与 −1 的 t=3.56 强确认不同,如实标注)。scope 仍 `|h|=1`(±1);`|h|≥2` 偏差结构不同、未验、不部署。承 v1.4 同诚实边界(提前于前向 S6 闸 + 用重估 δ,owner 决定);前向 S6 秋季照跑独立确认两线 δ。
- **v1.4 · 2026-07-09**:**C1 让球修正部署到服务端**(标准盘 `_model_board_handicap_lines` + 市场盘 `_market_handicap_lines`/`recommend_market_handicap`/`_market_reverse_handicap_probs` 四处,`implied_handicap_lines(c1=True)`,仅 `handicap_home=−1`)。**如实记两处偏离 v1.1 §4-S6 部署闸(据 owner 决定,选项 A)**:① **提前于前向 S6-FDR 闸**——本次凭 §H 历史 held-out(§H/H-S6 授权的检验)上线,前向 S6 秋季仍作**独立确认性、不因此免检**;② **用重估 δ=0.019 而非冻结 0.028**——v1.1 曾禁「按新数据改 δ 再宣称已验证」。**诚实缓和理由**:(a) 重估走**严格 train/test held-out**(500 Crown 前 5 月估 δ、后 5 月验),非「同数据估计+检验」;(b) 方向三数据集 + held-out 一致(全季 −1 线 N=2241 δ=DC−市场让胜 **+1.86pp** t=31.5;OOS 3-way log-loss 配对 **t=+3.56**);(c) 限 `|h|=1` 且**按线符号拆验** —— +1 线让胜无偏(t=−2.3),故 C1 **只对 −1** 生效(+1 的镜像偏「让负→让平」是**另立、未部署**);(d) 冻结 0.028 系 07-04 **in-sample**、被 −1 主导且乐观偏高,真 −1 δ≈1.86pp,故 0.019 是更佳估计。**判读地位**:C1 = **校准/防御性(防假软让胜)**,让球整体仍 null 无 +EV(HAD+HHAD × 11 联赛已证)→ 非新边缘。**前向 S6 权限不变**:秋季 forward S6-FDR 照跑;若给出显著不同 δ,按原「量级未定 → 下下季携新 δ」条款处理。源:`docs/jingcai_history_campaign_2026-07-09.md`、`记忆 500-historical-odds-archive`、scratchpad `c1_fit_oos.py`。
- **v1.3 · 2026-07-09**:新增 **§H 历史 held-out 分析**——缘于当日破解官方 `getFixedBonusV1` 公开历史竞彩全程走势(推翻 forward-only,`记忆 jingcai-fixedbonus-history-endpoint`)。§H 在**任何历史读数之前**声明:主窗口 2021–2025 + 全档案稳健、13 受训联赛、H-P1/H-P2(CLV)+ H-S family(m=5;S2 排除因 vote 无历史)、**复用 07-05 前冻结仪器逐字**、**单次预提交读数**、**历史 confirm≠钱(仍闸前向 P2)**、非 pristine(假设/选联赛有先验重叠)→强证据非终审。前向 §2–§5 窗口检验**未改一字**。此为**新增并行分析**,非对 §2–§5 的修改。
- **v1.2 · 2026-07-05**:S6 检验工具**提前建成并冻结**(原计划「秋季随数据补」,改为现在锁死以保可复现)。normative 实现:`nutmeg.v4.model.split_bias`(纯统计,复用 `clv_gate.mean_clv_test` 聚类 t)+ `nutmeg-s6-split-check` CLI(读自家缓存,`--since` 默认冻结窗口 2026-08-01,`--include-discovery` 在发现期数据上复现结论作锁验证)。**自检通过**:发现期数据上复现出让平 under +2.19pp / 让胜 over −2.13pp / 让负 −0.06pp、符号一致、锚完好、t=1.83(印证功率注记「t≈2 边缘」);默认窗口 N=0(秋季前无数据,正确)。冻结件(δ=0.028、窗口、|h|=1、残差公式)进 `test_s6_split_check` 回归线。**此为工具冻结,非规格变更**——S6 定义与 C1 的 δ 均未动,读数仍等秋季 forward 数据 + S-family BHY-FDR。
- **v1.1 · 2026-07-04**:新增 **S6 让球切分偏差复现**(S family m 5→6)+ **C1 修正式冻结与部署闸**(见 §4 S6 特别条款)。来源:同日三方回测附录(`docs/ah_vs_grid_three_way_backtest_2026-07-04.md`)发现网格在竞彩让球真实区(h≠0)让胜 +2.7pp/让平 −2.7pp/让负 ±0 的切分偏差(in-sample ≈2.5σ),EV 口径影响 让胜 +5pp(超闸门)/让平 −9.6pp。**修订先于窗口首条数据(窗口 2026-08-01 起,受训联赛尚未复赛),family 扩容不损 FDR 有效性;S6 检验的是窗口内新数据,发现样本(≤2026-07)不计入读数。**
- **v1.0 · 2026-07-02**:初版锁定。常数:`WARN_MIN_N=15 / CONFIRM_T=2.8 / CONFIRM_MIN_N=200 / FDR_Q=0.05 / _MIN_EV=0.05`;联赛全集 = 13(artifact 训练面);同 commit 修复联赛标签跨 writer 分裂(`league_labels.canonical_league` 进 `clv_ledger`,芬超/FIN_VEIKKAUSLIIGA 实测合一)。
